/**
 * External dependencies
 */
import React from 'react';

/**
 * Internal dependencies
 */

const CardBody: React.FunctionComponent = ({ children }) => {
  return (
    <div className="card-body">{children}</div>
  );
};

export { CardBody };
